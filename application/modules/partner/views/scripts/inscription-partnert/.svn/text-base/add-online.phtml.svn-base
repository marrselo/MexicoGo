<?php echo $this->navigation()->menu()->render();?>
<style>
    input[type="checkbox"]{
        display: block !important;  
    }
    </style>
<form method="post">
    <ul>
        <?php foreach ($this->banners as $index => $value): ?>
            <li><div><?php echo $index; ?></div>
                <ul>
                    <?php foreach ($value as $index2): ?>
                    <li><input type="checkbox" name="banner[]" value="<?php echo $index2['mexi_banners_id']; ?>" /><?php echo $index2['mexi_banners_nombre'] ?></li>
                    <?php endforeach; ?>    
                </ul>    
            </li>
        <?php endforeach; ?>
    </ul>
    <input type="submit" value="contactar" />
</form>